{%- comment -%}
  Hand-drawn lightning-bolt brand mark used in the footer, standing in for
  the Figma signature graphic. Swap for the real exported SVG whenever it's
  available — drop it in Assets and replace this snippet's <svg> contents.
{%- endcomment -%}
<svg
  class="icon icon-bolt{% if class %} {{ class }}{% endif %}"
  viewBox="0 0 60 160"
  fill="none"
  stroke="currentColor"
  stroke-width="10"
  stroke-linecap="round"
  stroke-linejoin="round"
  aria-hidden="true"
  focusable="false"
>
  <path d="M45 5 L10 85 L32 85 L15 155 L55 65 L30 65 Z"></path>
</svg>
